<?xml version="1.0"?>
<package format="3">
  <name>friday_terrain</name>
  <version>0.1.0</version>
  <description>
    Terrain-intelligence layer for Mark 1: reads the ground-scan RGB-D cloud,
    classifies each cell of terrain in front of the rover by slope + step height,
    and publishes both a Nav2-compatible cost map and a human-readable
    classified point cloud for the operator deck. The bridge between "the rover
    sees the ground" and "Nav2 decides where to drive".
  </description>
  <maintainer email="friday@friday-labs.example">Friday Labs</maintainer>
  <license>Apache-2.0</license>

  <depend>rclpy</depend>
  <depend>sensor_msgs</depend>
  <depend>sensor_msgs_py</depend>
  <depend>nav_msgs</depend>
  <depend>geometry_msgs</depend>
  <depend>std_msgs</depend>
  <depend>tf2_ros</depend>
  <depend>tf2_geometry_msgs</depend>
  <depend>numpy</depend>

  <test_depend>ament_pep257</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_python</build_type>
  </export>
</package>
